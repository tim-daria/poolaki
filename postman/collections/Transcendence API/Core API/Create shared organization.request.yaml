$kind: http-request
name: Create shared organization
url: "{{base_url}}/api/organizations/"
method: POST
headers:
  Content-Type: application/json
  X-CSRFToken: "{{csrf_token}}"
body:
  type: json
  content: |-
    {  "name": "Trip to France",
       "initial_balance": "1500.00"
    }
order: 3269326534608491
