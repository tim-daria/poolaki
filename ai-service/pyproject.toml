[project]
name = "poolaki-ai-service"

# ai-service version
version = "0.1.0"

# Minimum supported Python version
requires-python = ">=3.12"

# Later: pgvector, embedings and LLM client (when implemented)

dependencies = [
	"fastapi",
	"uvicorn[standard]",
	"httpx",
	"pydantic-settings",
]